<UserControl x:Class="Library.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
        <Button Content="Books" Command="{Binding NavigateBooksCommand}" Margin="10 2 50 2" FontSize="24"/>
        <Button Content="Authors" Command="{Binding NavigateAuthorsCommand}" Margin="20 2 50 2" FontSize="24"/>
        <Button Content="Loans" Command="{Binding NavigateLoansCommand}" Margin="20 2 50 2" FontSize="24"/>
    </StackPanel>
            
    </Grid>
</UserControl>
