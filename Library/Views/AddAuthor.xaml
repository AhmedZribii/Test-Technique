<UserControl x:Class="Library.Views.AddAuthor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Add Author" FontSize="30" Grid.Row="0"/>


        <Grid Grid.Row="1" Margin=" 0 25 0 0 " >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0 2 0 0" Text="First Name"/>
            <TextBox Grid.Row="1" Margin="0 5 0 0" Text="{Binding NewAuthor.FirstName, UpdateSourceTrigger=PropertyChanged}"/>

        </Grid>

        <Grid Grid.Row="2" Margin=" 0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0 2 0 0" Text="Last Name"/>
            <TextBox Grid.Row="1" Margin="0 5 0 0" Text="{Binding NewAuthor.LastName, UpdateSourceTrigger=PropertyChanged}"/>

        </Grid>
        
        <Grid Grid.Row="4"  Margin=" 0 10 0 0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Margin="0 2 0 0" Text="Date Of Birth"/>
            <DatePicker Grid.Row="1" Margin="0  5 0 0" SelectedDate="{Binding NewAuthor.DateOfBirth, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <StackPanel Grid.Row="5" Orientation="Horizontal"  Margin=" 0 10 0 0">
            <Button Content="Add" Margin="640 0 0 0" Command="{Binding AddAuthorCommand}"/>
            <Button Margin="5 0 0 0" Content="Cancel" Command="{Binding AuthorList}"/>
        </StackPanel>


    </Grid>
</UserControl>
