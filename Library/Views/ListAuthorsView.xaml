<UserControl x:Class="Library.Views.ListAuthorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Library.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="List of Authors" FontSize="24"/>
            <Button Grid.Column="1" Content="Add Author" Margin="10 0 0 0" Command="{Binding AddAuthor}"/>
        </Grid>
        <ListView Grid.Row="1" Margin="0 25 0 0" ItemsSource="{Binding Authors}" SelectedItem="{Binding SelectedAuthor}">
            <ListViewItem/>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="First Name" DisplayMemberBinding="{Binding FirstName}"/>
                    <GridViewColumn Header="Last Name" DisplayMemberBinding="{Binding LastName}"/>
                    <GridViewColumn Header="Date of birth" DisplayMemberBinding="{Binding DateOfBirth}"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 0 0 0">

            <Button Content="Update" Command="{Binding UpdateAuthorCommand}" IsEnabled="{Binding Path=SelectedAuthor, Converter={StaticResource NullToBoolConverter}}" Margin="10" />
            <Button Content="Delete" Command="{Binding DeleteAuthorCommand}" IsEnabled="{Binding Path=SelectedAuthor, Converter={StaticResource NullToBoolConverter}}" Margin="10" />
        </StackPanel>



    </Grid>
</UserControl>
